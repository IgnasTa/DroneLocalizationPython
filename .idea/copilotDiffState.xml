<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="CopilotDiffPersistence">
    <option name="pendingDiffs">
      <map>
        <entry key="/main (snapshot).py">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="/main (snapshot).py" />
              <option name="originalContent" value="from fastapi import FastAPI, UploadFile, File, Form&#10;import torch&#10;import cv2&#10;import os&#10;import math&#10;import numpy as np&#10;&#10;from DiscardSmallObjects import remove_small_objects&#10;from ExifMetadata import extrat_lat_lon&#10;from MatchToMap import predict&#10;from PredictLargeImage import predict_large_image&#10;from RotateImage import correct_yaw&#10;from SkeletonToGraph import skeleton_to_graph&#10;from Skeletonize import skeletonize_mask&#10;from UNetFormer import UNetFormer&#10;from YawExtract import extract_xmp_pure_python&#10;&#10;app = FastAPI()&#10;&#10;device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')&#10;model = UNetFormer().to(device)&#10;state_dict = torch.load(&quot;unet_former_with_moving_and_static_cars_as_road.pth&quot;, map_location=device)&#10;model.load_state_dict(state_dict)&#10;&#10;&#10;# @app.get(&quot;/&quot;)&#10;# async def root():&#10;#     photo_path =&quot;DJI_0444.jpg&quot;&#10;#     yaw, altitude = extract_xmp_pure_python(photo_path)&#10;#     drone_lat, drone_lon = extrat_lat_lon(photo_path)&#10;#     img = cv2.imread(photo_path)&#10;#     X, Y, _ = img.shape&#10;#     print(X,Y)&#10;#&#10;#     img, pred = predict_large_image(model, img, device)&#10;#&#10;#     mask = remove_small_objects(pred)&#10;#&#10;#     correct_yaw(mask, yaw)  # saves as &quot;drone_road_mask_rotated.png&quot;&#10;#     #&#10;#     skeletonize_mask(&quot;drone_road_mask_rotated.png&quot;, &quot;drone_road_skeleton_3346.tif&quot;)&#10;#     #&#10;#     graph = skeleton_to_graph(&quot;drone_road_skeleton_3346.tif&quot;)&#10;#     predict(drone_lat=drone_lat, drone_lon=drone_lon, altitude_m=altitude, image_width_px=Y, image_height_px=X,&#10;#             graph=graph)&#10;#     return {&quot;message&quot;: &quot;Hello World&quot;}&#10;&#10;&#10;@app.post(&quot;/&quot;)&#10;async def root(file: UploadFile = File(...)):&#10;    &quot;&quot;&quot;Upload an image and get aligned GPS coordinates&quot;&quot;&quot;&#10;    try:&#10;        addNoise = True&#10;        # 1. Save uploaded file&#10;        photo_path = f&quot;uploaded_{file.filename}&quot;&#10;        contents = await file.read()&#10;        with open(photo_path, &quot;wb&quot;) as f:&#10;            f.write(contents)&#10;        print(f&quot;✅ File saved: {photo_path}&quot;)&#10;&#10;        # 2. Extract metadata&#10;        yaw, altitude = extract_xmp_pure_python(photo_path)&#10;        drone_lat, drone_lon = extrat_lat_lon(photo_path)&#10;        drone_lat_noisy = drone_lat&#10;        drone_lon_noisy = drone_lon&#10;        print(f&quot;Original GPS: ({drone_lat:.6f}, {drone_lon:.6f}), Yaw: {yaw}°, Altitude: {altitude}m&quot;)&#10;&#10;        if addNoise:&#10;            # 3. Add random GPS error (~10-20m)&#10;            error_m = np.random.uniform(5, 12)  # Random error 10-20 meters&#10;            error_direction = np.random.uniform(0, 2 * np.pi)  # Random direction&#10;&#10;            # Convert meters to degrees&#10;            lat_error_deg = error_m * math.cos(error_direction) / 111000  # 1° lat ≈ 111km&#10;            lon_error_deg = error_m * math.sin(error_direction) / (111000 * math.cos(math.radians(drone_lat)))&#10;            print(f&quot;Added GPS error: {error_m:.1f}m at {math.degrees(error_direction):.1f}°&quot;)&#10;            print(f&quot;Error in degrees: lat={lat_error_deg:.8f}°, lon={lon_error_deg:.8f}°&quot;)&#10;            print(f&quot;Error in meters: lat={lat_error_deg * 111000:.2f}m, lon={lon_error_deg * (111000 * math.cos(math.radians(drone_lat))):.2f}m&quot;)&#10;&#10;            # Apply error&#10;            drone_lat_noisy = drone_lat + lat_error_deg&#10;            drone_lon_noisy = drone_lon + lon_error_deg&#10;&#10;&#10;        # 3. Read image and get dimensions&#10;        img = cv2.imread(photo_path)&#10;        X, Y, _ = img.shape&#10;        print(f&quot;Image dimensions: {Y}x{X} pixels&quot;)&#10;&#10;        # 4. Predict road mask&#10;        img, pred = predict_large_image(model, img, device)&#10;        mask = remove_small_objects(pred)&#10;&#10;        # 5. Rotate by yaw&#10;        correct_yaw(mask, yaw)&#10;&#10;        # 6. Skeletonize&#10;        skeletonize_mask(&quot;drone_road_mask_rotated.png&quot;, &quot;drone_road_skeleton_3346.tif&quot;)&#10;&#10;        # 7. Convert to graph&#10;        graph = skeleton_to_graph(&quot;drone_road_skeleton_3346.tif&quot;)&#10;&#10;        # 8. Match to OSM and get aligned coordinates&#10;        aligned_gdf = predict(&#10;            drone_lat=drone_lat_noisy,&#10;            drone_lon=drone_lon_noisy,&#10;            altitude_m=altitude,&#10;            image_width_px=Y,&#10;            image_height_px=X,&#10;            graph=graph&#10;        )&#10;&#10;        # 9. Extract aligned center coordinates&#10;        center_point = aligned_gdf.unary_union.centroid&#10;&#10;        aligned_lat = float(center_point.y)&#10;        aligned_lon = float(center_point.x)&#10;&#10;        # 10. Clean up&#10;        os.remove(photo_path)&#10;&#10;        return {&#10;            &quot;status&quot;: &quot;success&quot;,&#10;            &quot;original_gps&quot;: {&quot;lat&quot;: float(drone_lat), &quot;lon&quot;: float(drone_lon)},&#10;            &quot;aligned_gps&quot;: {&quot;lat&quot;: aligned_lat, &quot;lon&quot;: aligned_lon},&#10;            &quot;aligned_center&quot;: f&quot;{aligned_lat:.6f}, {aligned_lon:.6f}&quot;&#10;        }&#10;&#10;    except Exception as e:&#10;        return {&quot;status&quot;: &quot;error&quot;, &quot;message&quot;: str(e)}&#10;&#10;&#10;@app.get(&quot;/hello/{name}&quot;)&#10;async def say_hello(name: str):&#10;    return {&quot;message&quot;: f&quot;Hello {name}&quot;}&#10;&#10;#&#10;@app.post(&quot;/video&quot;)&#10;async def photoFromVideo(file: UploadFile = File(...)):&#10;    &quot;&quot;&quot;Upload an image and get aligned GPS coordinates&quot;&quot;&quot;&#10;    try:&#10;        addNoise = True&#10;        # 1. Save uploaded file&#10;        photo_path = f&quot;uploaded_{file.filename}&quot;&#10;        contents = await file.read()&#10;        with open(photo_path, &quot;wb&quot;) as f:&#10;            f.write(contents)&#10;        print(f&quot;✅ File saved: {photo_path}&quot;)&#10;&#10;        # 2. Extract metadata&#10;        yaw, altitude = extract_xmp_pure_python(photo_path)&#10;        drone_lat, drone_lon = extrat_lat_lon(photo_path)&#10;        drone_lat_noisy = drone_lat&#10;        drone_lon_noisy = drone_lon&#10;        print(f&quot;Original GPS: ({drone_lat:.6f}, {drone_lon:.6f}), Yaw: {yaw}°, Altitude: {altitude}m&quot;)&#10;&#10;        if addNoise:&#10;            # 3. Add random GPS error (~10-20m)&#10;            error_m = np.random.uniform(5, 12)  # Random error 10-20 meters&#10;            error_direction = np.random.uniform(0, 2 * np.pi)  # Random direction&#10;&#10;            # Convert meters to degrees&#10;            lat_error_deg = error_m * math.cos(error_direction) / 111000  # 1° lat ≈ 111km&#10;            lon_error_deg = error_m * math.sin(error_direction) / (111000 * math.cos(math.radians(drone_lat)))&#10;            print(f&quot;Added GPS error: {error_m:.1f}m at {math.degrees(error_direction):.1f}°&quot;)&#10;            print(f&quot;Error in degrees: lat={lat_error_deg:.8f}°, lon={lon_error_deg:.8f}°&quot;)&#10;            print(&#10;                f&quot;Error in meters: lat={lat_error_deg * 111000:.2f}m, lon={lon_error_deg * (111000 * math.cos(math.radians(drone_lat))):.2f}m&quot;)&#10;&#10;            # Apply error&#10;            drone_lat_noisy = drone_lat + lat_error_deg&#10;            drone_lon_noisy = drone_lon + lon_error_deg&#10;&#10;        # 3. Read image and get dimensions&#10;        img = cv2.imread(photo_path)&#10;        X, Y, _ = img.shape&#10;        print(f&quot;Image dimensions: {Y}x{X} pixels&quot;)&#10;&#10;        # 4. Predict road mask&#10;        img, pred = predict_large_image(model, img, device)&#10;        mask = remove_small_objects(pred)&#10;&#10;        # 5. Rotate by yaw&#10;        correct_yaw(mask, yaw)&#10;&#10;        # 6. Skeletonize&#10;        skeletonize_mask(&quot;drone_road_mask_rotated.png&quot;, &quot;drone_road_skeleton_3346.tif&quot;)&#10;&#10;        # 7. Convert to graph&#10;        graph = skeleton_to_graph(&quot;drone_road_skeleton_3346.tif&quot;)&#10;&#10;        # 8. Match to OSM and get aligned coordinates&#10;        aligned_gdf = predict(&#10;            drone_lat=drone_lat_noisy,&#10;            drone_lon=drone_lon_noisy,&#10;            altitude_m=altitude,&#10;            image_width_px=Y,&#10;            image_height_px=X,&#10;            graph=graph&#10;        )&#10;&#10;        # 9. Extract aligned center coordinates&#10;        center_point = aligned_gdf.unary_union.centroid&#10;&#10;        aligned_lat = float(center_point.y)&#10;        aligned_lon = float(center_point.x)&#10;&#10;        # 10. Clean up&#10;        os.remove(photo_path)&#10;&#10;        return {&#10;            &quot;status&quot;: &quot;success&quot;,&#10;            &quot;original_gps&quot;: {&quot;lat&quot;: float(drone_lat), &quot;lon&quot;: float(drone_lon)},&#10;            &quot;aligned_gps&quot;: {&quot;lat&quot;: aligned_lat, &quot;lon&quot;: aligned_lon},&#10;            &quot;aligned_center&quot;: f&quot;{aligned_lat:.6f}, {aligned_lon:.6f}&quot;&#10;        }&#10;&#10;    except Exception as e:&#10;        return {&quot;status&quot;: &quot;error&quot;, &quot;message&quot;: str(e)}&#10;" />
              <option name="updatedContent" value="from fastapi import FastAPI, UploadFile, File, Form&#10;import torch&#10;import cv2&#10;import os&#10;import math&#10;import numpy as np&#10;&#10;from DiscardSmallObjects import remove_small_objects&#10;from ExifMetadata import extrat_lat_lon&#10;from MatchToMap import predict&#10;from PredictLargeImage import predict_large_image&#10;from RotateImage import correct_yaw&#10;from SkeletonToGraph import skeleton_to_graph&#10;from Skeletonize import skeletonize_mask&#10;from UNetFormer import UNetFormer&#10;from YawExtract import extract_xmp_pure_python&#10;&#10;app = FastAPI()&#10;&#10;device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')&#10;model = UNetFormer().to(device)&#10;state_dict = torch.load(&quot;unet_former_with_moving_and_static_cars_as_road.pth&quot;, map_location=device)&#10;model.load_state_dict(state_dict)&#10;&#10;&#10;# @app.get(&quot;/&quot;)&#10;# async def root():&#10;#     photo_path =&quot;DJI_0444.jpg&quot;&#10;#     yaw, altitude = extract_xmp_pure_python(photo_path)&#10;#     drone_lat, drone_lon = extrat_lat_lon(photo_path)&#10;#     img = cv2.imread(photo_path)&#10;#     X, Y, _ = img.shape&#10;#     print(X,Y)&#10;#&#10;#     img, pred = predict_large_image(model, img, device)&#10;#&#10;#     mask = remove_small_objects(pred)&#10;#&#10;#     correct_yaw(mask, yaw)  # saves as &quot;drone_road_mask_rotated.png&quot;&#10;#     #&#10;#     skeletonize_mask(&quot;drone_road_mask_rotated.png&quot;, &quot;drone_road_skeleton_3346.tif&quot;)&#10;#     #&#10;#     graph = skeleton_to_graph(&quot;drone_road_skeleton_3346.tif&quot;)&#10;#     predict(drone_lat=drone_lat, drone_lon=drone_lon, altitude_m=altitude, image_width_px=Y, image_height_px=X,&#10;#             graph=graph)&#10;#     return {&quot;message&quot;: &quot;Hello World&quot;}&#10;&#10;&#10;@app.post(&quot;/&quot;)&#10;async def root(file: UploadFile = File(...)):&#10;    &quot;&quot;&quot;Upload an image and get aligned GPS coordinates&quot;&quot;&quot;&#10;    try:&#10;        addNoise = True&#10;        # 1. Save uploaded file&#10;        photo_path = f&quot;uploaded_{file.filename}&quot;&#10;        contents = await file.read()&#10;        with open(photo_path, &quot;wb&quot;) as f:&#10;            f.write(contents)&#10;        print(f&quot;✅ File saved: {photo_path}&quot;)&#10;&#10;        # 2. Extract metadata&#10;        yaw, altitude = extract_xmp_pure_python(photo_path)&#10;        drone_lat, drone_lon = extrat_lat_lon(photo_path)&#10;        drone_lat_noisy = drone_lat&#10;        drone_lon_noisy = drone_lon&#10;        print(f&quot;Original GPS: ({drone_lat:.6f}, {drone_lon:.6f}), Yaw: {yaw}°, Altitude: {altitude}m&quot;)&#10;&#10;        if addNoise:&#10;            # 3. Add random GPS error (~10-20m)&#10;            error_m = np.random.uniform(5, 12)  # Random error 10-20 meters&#10;            error_direction = np.random.uniform(0, 2 * np.pi)  # Random direction&#10;&#10;            # Convert meters to degrees&#10;            lat_error_deg = error_m * math.cos(error_direction) / 111000  # 1° lat ≈ 111km&#10;            lon_error_deg = error_m * math.sin(error_direction) / (111000 * math.cos(math.radians(drone_lat)))&#10;            print(f&quot;Added GPS error: {error_m:.1f}m at {math.degrees(error_direction):.1f}°&quot;)&#10;            print(f&quot;Error in degrees: lat={lat_error_deg:.8f}°, lon={lon_error_deg:.8f}°&quot;)&#10;            print(f&quot;Error in meters: lat={lat_error_deg * 111000:.2f}m, lon={lon_error_deg * (111000 * math.cos(math.radians(drone_lat))):.2f}m&quot;)&#10;&#10;            # Apply error&#10;            drone_lat_noisy = drone_lat + lat_error_deg&#10;            drone_lon_noisy = drone_lon + lon_error_deg&#10;&#10;&#10;        # 3. Read image and get dimensions&#10;        img = cv2.imread(photo_path)&#10;        X, Y, _ = img.shape&#10;        print(f&quot;Image dimensions: {Y}x{X} pixels&quot;)&#10;&#10;        # 4. Predict road mask&#10;        img, pred = predict_large_image(model, img, device)&#10;        mask = remove_small_objects(pred)&#10;&#10;        # 5. Rotate by yaw&#10;        correct_yaw(mask, yaw)&#10;&#10;        # 6. Skeletonize&#10;        skeletonize_mask(&quot;drone_road_mask_rotated.png&quot;, &quot;drone_road_skeleton_3346.tif&quot;)&#10;&#10;        # 7. Convert to graph&#10;        graph = skeleton_to_graph(&quot;drone_road_skeleton_3346.tif&quot;)&#10;&#10;        # 8. Match to OSM and get aligned coordinates&#10;        aligned_gdf = predict(&#10;            drone_lat=drone_lat_noisy,&#10;            drone_lon=drone_lon_noisy,&#10;            altitude_m=altitude,&#10;            image_width_px=Y,&#10;            image_height_px=X,&#10;            graph=graph&#10;        )&#10;&#10;        # 9. Extract aligned center coordinates&#10;        center_point = aligned_gdf.unary_union.centroid&#10;&#10;        aligned_lat = float(center_point.y)&#10;        aligned_lon = float(center_point.x)&#10;&#10;        # 10. Clean up&#10;        os.remove(photo_path)&#10;&#10;        return {&#10;            &quot;status&quot;: &quot;success&quot;,&#10;            &quot;original_gps&quot;: {&quot;lat&quot;: float(drone_lat), &quot;lon&quot;: float(drone_lon)},&#10;            &quot;aligned_gps&quot;: {&quot;lat&quot;: aligned_lat, &quot;lon&quot;: aligned_lon},&#10;            &quot;aligned_center&quot;: f&quot;{aligned_lat:.6f}, {aligned_lon:.6f}&quot;&#10;        }&#10;&#10;    except Exception as e:&#10;        return {&quot;status&quot;: &quot;error&quot;, &quot;message&quot;: str(e)}&#10;&#10;&#10;@app.get(&quot;/hello/{name}&quot;)&#10;async def say_hello(name: str):&#10;    return {&quot;message&quot;: f&quot;Hello {name}&quot;}&#10;&#10;#&#10;@app.post(&quot;/video&quot;)&#10;async def photoFromVideo(file: UploadFile = File(...), lat: float = Form(...), lon: float = Form(...), altitude: float = Form(...)):&#10;    &quot;&quot;&quot;Upload an image and get aligned GPS coordinates&quot;&quot;&quot;&#10;    try:&#10;        addNoise = True&#10;        # 1. Save uploaded file&#10;        photo_path = f&quot;uploaded_{file.filename}&quot;&#10;        contents = await file.read()&#10;        with open(photo_path, &quot;wb&quot;) as f:&#10;            f.write(contents)&#10;        print(f&quot;✅ File saved: {photo_path}&quot;)&#10;&#10;        # 2. Extract yaw from metadata, use provided lat, lon, altitude&#10;        yaw, _ = extract_xmp_pure_python(photo_path)&#10;        drone_lat = lat&#10;        drone_lon = lon&#10;        drone_lat_noisy = drone_lat&#10;        drone_lon_noisy = drone_lon&#10;        print(f&quot;Provided GPS: ({drone_lat:.6f}, {drone_lon:.6f}), Yaw: {yaw}°, Altitude: {altitude}m&quot;)&#10;&#10;        if addNoise:&#10;            # 3. Add random GPS error (~10-20m)&#10;            error_m = np.random.uniform(5, 12)  # Random error 10-20 meters&#10;            error_direction = np.random.uniform(0, 2 * np.pi)  # Random direction&#10;&#10;            # Convert meters to degrees&#10;            lat_error_deg = error_m * math.cos(error_direction) / 111000  # 1° lat ≈ 111km&#10;            lon_error_deg = error_m * math.sin(error_direction) / (111000 * math.cos(math.radians(drone_lat)))&#10;            print(f&quot;Added GPS error: {error_m:.1f}m at {math.degrees(error_direction):.1f}°&quot;)&#10;            print(f&quot;Error in degrees: lat={lat_error_deg:.8f}°, lon={lon_error_deg:.8f}°&quot;)&#10;            print(f&quot;Error in meters: lat={lat_error_deg * 111000:.2f}m, lon={lon_error_deg * (111000 * math.cos(math.radians(drone_lat))):.2f}m&quot;)&#10;&#10;            # Apply error&#10;            drone_lat_noisy = drone_lat + lat_error_deg&#10;            drone_lon_noisy = drone_lon + lon_error_deg&#10;&#10;        # 3. Read image and get dimensions&#10;        img = cv2.imread(photo_path)&#10;        X, Y, _ = img.shape&#10;        print(f&quot;Image dimensions: {Y}x{X} pixels&quot;)&#10;&#10;        # 4. Predict road mask&#10;        img, pred = predict_large_image(model, img, device)&#10;        mask = remove_small_objects(pred)&#10;&#10;        # 5. Rotate by yaw&#10;        correct_yaw(mask, yaw)&#10;&#10;        # 6. Skeletonize&#10;        skeletonize_mask(&quot;drone_road_mask_rotated.png&quot;, &quot;drone_road_skeleton_3346.tif&quot;)&#10;&#10;        # 7. Convert to graph&#10;        graph = skeleton_to_graph(&quot;drone_road_skeleton_3346.tif&quot;)&#10;&#10;        # 8. Match to OSM and get aligned coordinates&#10;        aligned_gdf = predict(&#10;            drone_lat=drone_lat_noisy,&#10;            drone_lon=drone_lon_noisy,&#10;            altitude_m=altitude,&#10;            image_width_px=Y,&#10;            image_height_px=X,&#10;            graph=graph&#10;        )&#10;&#10;        # 9. Extract aligned center coordinates&#10;        center_point = aligned_gdf.unary_union.centroid&#10;&#10;        aligned_lat = float(center_point.y)&#10;        aligned_lon = float(center_point.x)&#10;&#10;        # 10. Clean up&#10;        os.remove(photo_path)&#10;&#10;        return {&#10;            &quot;status&quot;: &quot;success&quot;,&#10;            &quot;original_gps&quot;: {&quot;lat&quot;: float(drone_lat), &quot;lon&quot;: float(drone_lon)},&#10;            &quot;aligned_gps&quot;: {&quot;lat&quot;: aligned_lat, &quot;lon&quot;: aligned_lon},&#10;            &quot;aligned_center&quot;: f&quot;{aligned_lat:.6f}, {aligned_lon:.6f}&quot;&#10;        }&#10;&#10;    except Exception as e:&#10;        return {&quot;status&quot;: &quot;error&quot;, &quot;message&quot;: str(e)}" />
            </PendingDiffInfo>
          </value>
        </entry>
      </map>
    </option>
  </component>
</project>